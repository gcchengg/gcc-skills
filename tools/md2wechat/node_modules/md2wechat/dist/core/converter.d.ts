/**
 * HTML converter module for WeChat Official Account
 */
/**
 * Converter options
 */
export interface ConverterOptions {
    theme?: string;
    codeTheme?: string;
    themeBasePath?: string;
}
/**
 * HTML converter class
 */
export declare class Converter {
    private themeName;
    private codeThemeName;
    private themeBasePath;
    constructor(options?: ConverterOptions);
    /**
     * Load CSS files
     */
    private loadCSS;
    /**
     * Convert HTML to WeChat compatible format with inlined CSS
     */
    convert(html: string): Promise<string>;
    /**
     * Wrap HTML content in container
     */
    wrapContent(content: string): string;
    /**
     * Process HTML with conversion and wrapping
     */
    process(html: string): Promise<string>;
    /**
     * Save HTML to file
     */
    saveToFile(html: string, filePath: string): Promise<void>;
    /**
     * Convert markdown file to HTML file
     */
    convertFile(inputPath: string, outputPath: string): Promise<void>;
}
/**
 * Convert HTML to WeChat format (convenience function)
 */
export declare function convertToWeChat(html: string, options?: ConverterOptions): Promise<string>;
/**
 * Wrap HTML content (convenience function)
 */
export declare function wrapContent(content: string): string;
declare const _default: {
    Converter: typeof Converter;
    convertToWeChat: typeof convertToWeChat;
    wrapContent: typeof wrapContent;
};
export default _default;
//# sourceMappingURL=converter.d.ts.map