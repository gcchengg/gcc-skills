/**
 * WeChat Official Account API module
 */
import type { WeChatConfig, ArticleData, DraftItem, UploadMaterialResponse, CreateDraftResponse } from '../types/index.js';
/**
 * WeChat Official Account API class
 */
export declare class WeChatApi {
    private config;
    private apiBaseUrl;
    private accessToken;
    private tokenExpireTime;
    constructor(config: WeChatConfig);
    /**
     * Make HTTPS request to WeChat API
     */
    private request;
    /**
     * Token expiry buffer in milliseconds (5 minutes)
     */
    private static readonly TOKEN_EXPIRY_BUFFER_MS;
    /**
     * Token expiry buffer in seconds (5 minutes)
     */
    private static readonly TOKEN_EXPIRY_BUFFER_SEC;
    /**
     * Convert ArticleData from camelCase to snake_case for WeChat API
     */
    private convertArticleToSnakeCase;
    /**
     * Get access token
     */
    getAccessToken(): Promise<string>;
    /**
     * Upload permanent material (image)
     * Returns media_id and url
     */
    uploadImage(filePath: string): Promise<UploadMaterialResponse>;
    /**
     * Upload thumbnail
     */
    uploadThumb(filePath: string): Promise<string>;
    /**
     * Create new draft
     */
    createDraft(articles: ArticleData[]): Promise<CreateDraftResponse>;
    /**
     * Update existing draft
     */
    updateDraft(mediaId: string, index: number, article: ArticleData): Promise<void>;
    /**
     * Get draft list
     */
    getDraftList(offset?: number, count?: number, noContent?: number): Promise<{
        itemCount: number;
        item: DraftItem[];
    }>;
    /**
     * Add permanent news material
     */
    addNews(articles: ArticleData[]): Promise<{
        mediaId: string;
    }>;
}
export default WeChatApi;
//# sourceMappingURL=wechat-api.d.ts.map