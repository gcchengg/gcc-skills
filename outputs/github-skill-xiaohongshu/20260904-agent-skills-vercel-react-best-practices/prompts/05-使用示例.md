3:4 竖版中文艺术画册内页，延续冷白厚棉纸、炭黑+电光绿、右上柔光、击凸/压凹。标题“两次 await，一次等完”，左下“REACT / 05”。左右代码纸张对照。左标“串行”：“const user = await getUser()\nconst posts = await getPosts()”，两段上下排列；右标“并行”：“const [user, posts] = await Promise.all([\n getUser(), getPosts()\n])”，两条绿色路径并行合流。中间标签“async-parallel”。底部：“前提：两个请求互不依赖”。代码拼写准确，手机可读。禁止金属、霓虹、渐变、卡通、PPT 模板、水印。
