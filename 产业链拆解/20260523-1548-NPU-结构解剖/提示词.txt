生成一张“NPU技术结构解剖信息图”，使用 gpt-image-2，3072x4096，3:4 竖版小红书海报比例，high quality，PNG 输出，4K ultra high resolution，高清锐利细节，信息图文字在放大后依然清晰可读且不模糊，优先大字号标题、清晰模块名、低文字密度标注、避免微小文字，要求高对比文字区域、锐利边缘渲染、干净的信息图分隔线，风格参考人体解剖图、工业剖面图、芯片架构图、系统架构图、工程教学挂图和博物馆科技图谱。

将 NPU 像面向神经网络推理的专用计算器官一样一层一层拆开，做成芯片架构剖面图、张量计算流向图、模型部署链路图和局部放大图。

画面中心展示 NPU 从模型输入到推理输出的分层结构：模型编译与算子映射，张量/矩阵计算阵列，片上 SRAM 与缓存，数据搬运与 DMA，量化与低精度计算模块，控制调度单元，安全与隔离模块，系统总线与 SoC 接口，软件 SDK 与推理框架，典型应用场景（手机端 AI、智能座舱、摄像头、IoT、边缘推理）。

每一层都要被单独分离出来，悬浮排列，层级清楚。用箭头表现“模型 -> 编译 -> 算子切分 -> 张量计算 -> 片上缓存 -> 输出结果”的数据流，用不同颜色线条区分模型流、特征图数据流、权重读取流、控制流和能耗热流。

每个模块旁边设置大字号可读标注，说明模块负责什么、依赖哪些上游、如何产生能效价值、主要成本或瓶颈在哪里，例如算子支持范围、模型兼容性、内存带宽、片上缓存容量、低精度精度损失、工具链成熟度、端侧功耗和部署门槛。所有标注必须控制数量并放大字号，确保放大查看时仍清晰，不要使用拥挤小字。

整体要有强烈的科学展示感、工程感和商业洞察感，像顶级科技研究机构制作的 NPU 教学海报。视觉风格精密、清楚、理性、有秩序，构图像学术信息图，强调“神经网络加速 + 端侧推理 + 能效比 + 工具链瓶颈”。

采用高级纸张或冷静科技图谱背景，细腻线描，真实芯片硅片、张量阵列、片上缓存和 SoC 接口材质，少量蓝灰、银白、黑色、绿色数据流作为辅助色。不要赛博朋克霓虹，不要空泛未来感，不要只画芯片和光线。

请让版面清晰分区，包括：整体架构、张量计算阵列、片上存储、模型编译、系统接口、端侧应用、能耗结构、软件生态、成本结构、风险瓶颈。整体版式必须优先 3:4 竖版 3072x4096 小红书 4K 海报构图，适合手机端浏览和发布；不要为了排满画面牺牲文字可读性，放大查看时文字边缘必须清晰锐利、不发虚、不糊成色块。

不要人物肖像，不要夸张机器人，不要无关装饰，不要营销海报风。重点是“NPU 专用加速 + 模型部署 + 片上存储 + 能效瓶颈”。
