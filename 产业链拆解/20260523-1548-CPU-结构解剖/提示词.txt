生成一张“CPU技术结构解剖信息图”，使用 gpt-image-2，3072x4096，3:4 竖版小红书海报比例，high quality，PNG 输出，4K ultra high resolution，高清锐利细节，信息图文字在放大后依然清晰可读且不模糊，优先大字号标题、清晰模块名、低文字密度标注、避免微小文字，要求高对比文字区域、锐利边缘渲染、干净的信息图分隔线，风格参考人体解剖图、工业剖面图、芯片架构图、系统架构图、工程教学挂图和博物馆科技图谱。

将 CPU 像复杂机械或生物系统一样一层一层拆开，做成清晰的芯片架构剖面图、模块爆炸分解图、指令流向图和局部放大图。

画面中心展示 CPU 从输入指令到计算输出的分层结构：指令集与软件接口层，取指与译码单元，执行单元（整数、浮点、向量、分支处理），寄存器与流水线，缓存层级（L1、L2、L3 类别化表现），内存控制器与 I/O 接口，时钟与电源管理，安全与虚拟化支持，封装与散热接口，典型应用场景（操作系统、服务器、PC、嵌入式、云计算）。

每一层都要被单独分离出来，悬浮排列，层级清楚，像医学解剖图那样一寸一寸拆开。用箭头表现“指令 -> 译码 -> 执行 -> 缓存 -> 内存 -> 输出”的数据流，用不同颜色线条区分控制流、数据流、访存路径和电源热流。

每个模块旁边设置大字号可读标注，说明模块负责什么、依赖哪些上游、如何产生算力价值、主要成本或瓶颈在哪里，例如单核性能、功耗墙、缓存命中率、内存延迟、制程与面积、分支预测、散热和生态兼容性。所有标注必须控制数量并放大字号，确保放大查看时仍清晰，不要使用拥挤小字。

整体要有强烈的科学展示感、工程感和商业洞察感，像顶级科技研究机构制作的芯片教学海报。视觉风格精密、清楚、理性、有秩序，构图像学术信息图，强调“结构拆解 + 功能解释 + 数据流动 + 性能瓶颈”。

采用高级纸张或冷静科技图谱背景，细腻线描，真实芯片硅片、金属互连和封装材质，少量蓝灰、银白、黑色、绿色数据流作为辅助色。不要赛博朋克霓虹，不要空泛未来感，不要只画芯片和光线。

请让版面清晰分区，包括：整体架构、核心执行单元、缓存与内存、控制与调度、I/O 接口、软件生态、性能指标、成本结构、风险瓶颈、应用场景。整体版式必须优先 3:4 竖版 3072x4096 小红书 4K 海报构图，适合手机端浏览和发布；不要为了排满画面牺牲文字可读性，放大查看时文字边缘必须清晰锐利、不发虚、不糊成色块。

不要人物肖像，不要夸张机器人，不要无关装饰，不要营销海报风。重点是“CPU 内部架构 + 指令执行 + 缓存层级 + 性能瓶颈”。
