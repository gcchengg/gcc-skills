生成一张“GPU技术结构解剖信息图”，使用 gpt-image-2，3072x4096，3:4 竖版小红书海报比例，high quality，PNG 输出，4K ultra high resolution，高清锐利细节，信息图文字在放大后依然清晰可读且不模糊，优先大字号标题、清晰模块名、低文字密度标注、避免微小文字，要求高对比文字区域、锐利边缘渲染、干净的信息图分隔线，风格参考人体解剖图、工业剖面图、芯片架构图、系统架构图、工程教学挂图和博物馆科技图谱。

将 GPU 像复杂并行计算机器一样一层一层拆开，做成清晰的芯片架构剖面图、并行核心阵列图、显存带宽流向图和局部放大图。

画面中心展示 GPU 从任务调度到并行输出的分层结构：任务调度与命令处理，流式多处理器/计算单元阵列，向量与矩阵计算单元，纹理与图形管线，片上缓存与共享存储，高带宽显存接口，互连与 I/O，驱动与编程框架，散热与供电，典型应用场景（图形渲染、AI 训练推理、科学计算、视频处理、云计算）。

每一层都要被单独分离出来，悬浮排列，层级清楚。用箭头表现“任务批量输入 -> 并行调度 -> 计算单元阵列 -> 显存读写 -> 输出结果”的数据流，用不同颜色线条区分图形渲染流、AI 矩阵计算流、显存带宽流和热流。

每个模块旁边设置大字号可读标注，说明模块负责什么、依赖哪些上游、如何产生并行算力价值、主要成本或瓶颈在哪里，例如并行度、显存容量、显存带宽、互连、功耗、散热、软件生态、利用率和供应链约束。所有标注必须控制数量并放大字号，确保放大查看时仍清晰，不要使用拥挤小字。

整体要有强烈的科学展示感、工程感和商业洞察感，像顶级科技研究机构制作的 GPU 教学海报。视觉风格精密、清楚、理性、有秩序，构图像学术信息图，强调“并行架构 + 数据吞吐 + 显存带宽 + 算力瓶颈”。

采用高级纸张或冷静科技图谱背景，细腻线描，真实芯片硅片、计算核心阵列、显存颗粒和散热结构材质，少量蓝灰、银白、黑色、绿色数据流作为辅助色。不要赛博朋克霓虹，不要空泛未来感，不要只画芯片和光线。

请让版面清晰分区，包括：整体架构、计算核心、显存系统、图形管线、AI 计算路径、软件生态、供电散热、商业化路径、成本结构、风险瓶颈。整体版式必须优先 3:4 竖版 3072x4096 小红书 4K 海报构图，适合手机端浏览和发布；不要为了排满画面牺牲文字可读性，放大查看时文字边缘必须清晰锐利、不发虚、不糊成色块。

不要人物肖像，不要夸张机器人，不要无关装饰，不要营销海报风。重点是“GPU 并行架构 + 显存带宽 + 软件生态 + 算力瓶颈”。
