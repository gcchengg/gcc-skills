生成一张“容器与Kubernetes技术结构解剖信息图”，8K ultra high resolution，竖版小红书海报比例，高清锐利细节，文字区域清晰可读，风格参考人体解剖图、工业剖面图、系统架构图、工程教学挂图和博物馆科技图谱。

将容器与Kubernetes像一套把应用打包、调度、扩缩容和自愈的云原生操作系统一样拆开，做成爆炸分解图、集群剖面图、调度流程图和服务治理图。画面中心展示：应用代码层、容器镜像层、镜像仓库层、Pod层、Node节点层、Kubernetes控制平面层、调度器层、Service网络层、Ingress网关层、ConfigMap/Secret配置层、存储卷层、监控日志层、安全策略层、CI/CD交付层。

突出三条流动路径：交付流从代码构建镜像并发布到集群；调度流由控制平面把Pod分配到节点；服务流通过Service、Ingress、网络策略和负载均衡对外提供能力。

每个模块旁边加入引线标注，说明功能、依赖、价值和瓶颈。重点标注：容器解决环境一致性，Kubernetes解决大规模容器编排；核心瓶颈包括学习成本、网络复杂度、状态服务管理、权限安全、资源浪费、排障难度和集群运维。

视觉元素包括容器盒子、镜像仓库、Pod蜂窝、Node服务器、控制平面、调度箭头、服务网关、监控面板。采用蓝灰、银白、黑色、绿色数据流，不要人物肖像，不要营销海报风，不要赛博朋克霓虹。
